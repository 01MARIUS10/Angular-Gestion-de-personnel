import { UserFunctionnalityPipe } from './user-functionnality.pipe';

describe('UserFunctionnalityPipe', () => {
  it('create an instance', () => {
    const pipe = new UserFunctionnalityPipe();
    expect(pipe).toBeTruthy();
  });
});
